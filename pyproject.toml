[project]
name = "llmmigrate"
version = "0.1.0"
description = "AI tools for migrating code"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "langchain-community>=0.3.14",
    "langchain-google-genai>=2.0.8",
    "langchain-openai>=0.3.0",
    "sciris>=3.2.0",
]

[build-system]
requires = ["flit-core>=3.7"]
build-backend = "flit_core.buildapi"
